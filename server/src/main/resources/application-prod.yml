spring:
  datasource:
    url: ${COLLA_DATASOURCE_URL}
    username: ${COLLA_DATASOURCE_USERNAME}
    password: ${COLLA_DATASOURCE_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      connect-timeout: ${REDIS_CONNECT_TIMEOUT:2s}
      timeout: ${REDIS_COMMAND_TIMEOUT:2s}

colla:
  runtime:
    role: ${COLLA_RUNTIME_ROLE}
  realtime:
    channel: ${COLLA_REALTIME_CHANNEL:colla:prod:realtime:v1}
    max-connections: ${COLLA_REALTIME_MAX_CONNECTIONS:5000}
    legacy-frames-enabled: ${COLLA_REALTIME_LEGACY_FRAMES_ENABLED:false}
  storage:
    endpoint: ${MINIO_ENDPOINT}
    access-key: ${MINIO_ACCESS_KEY}
    secret-key: ${MINIO_SECRET_KEY}
    bucket: ${MINIO_BUCKET}
    max-upload-size-bytes: ${MAX_UPLOAD_SIZE_BYTES:104857600}
  security:
    access-secret: ${JWT_ACCESS_SECRET}
    refresh-secret: ${JWT_REFRESH_SECRET}
    access-token-ttl-minutes: ${JWT_ACCESS_TOKEN_TTL_MINUTES:30}
    refresh-token-ttl-days: ${JWT_REFRESH_TOKEN_TTL_DAYS:30}
  web-security:
    cors-allowed-origins: ${CORS_ALLOWED_ORIGINS}
  init-admin:
    username: ${INIT_ADMIN_USERNAME}
    password: ${INIT_ADMIN_PASSWORD}
    display-name: ${INIT_ADMIN_DISPLAY_NAME}
    email: ${INIT_ADMIN_EMAIL}
  project-space-experience:
    enabled: ${COLLA_PROJECT_SPACE_EXPERIENCE_ENABLED:false}
    kill-switch: ${COLLA_PROJECT_SPACE_EXPERIENCE_KILL_SWITCH:true}
    policy-version: ${COLLA_PROJECT_SPACE_EXPERIENCE_POLICY_VERSION:s21-m7-v1}
    rollout-basis-points: ${COLLA_PROJECT_SPACE_EXPERIENCE_ROLLOUT_BASIS_POINTS:0}
    evaluation-salt: ${COLLA_PROJECT_SPACE_EXPERIENCE_EVALUATION_SALT:s21-m7-production-disabled}
    cache-max-age-seconds: ${COLLA_PROJECT_SPACE_EXPERIENCE_CACHE_MAX_AGE_SECONDS:0}
    telemetry:
      enabled: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_ENABLED:false}
      sample-basis-points: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_SAMPLE_BASIS_POINTS:0}
      max-batch-size: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_MAX_BATCH_SIZE:20}
