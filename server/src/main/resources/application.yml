spring:
  application:
    name: colla-platform
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/colla_platform
    username: colla
    password: colla_dev_password
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        session:
          events:
            log:
              LOG_QUERIES_SLOWER_THAN_MS: 500
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  port: 8080
  shutdown: graceful
  error:
    include-path: never

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,runtimeRoleReadiness,domainEventWorkerReadiness,realtimeRedisReadiness
        liveness:
          include: livenessState

springdoc:
  swagger-ui:
    path: /swagger-ui

colla:
  runtime:
    role: ${COLLA_RUNTIME_ROLE:}
    instance-id: ${COLLA_INSTANCE_ID:${HOSTNAME:local}}
    version: ${COLLA_VERSION:0.1.0}
    commit: ${SOURCE_COMMIT:unknown}
  realtime:
    channel: ${COLLA_REALTIME_CHANNEL:colla:local:realtime:v1}
    max-payload-bytes: ${COLLA_REALTIME_MAX_PAYLOAD_BYTES:16384}
    send-threads: ${COLLA_REALTIME_SEND_THREADS:4}
    executor-queue-capacity: ${COLLA_REALTIME_EXECUTOR_QUEUE_CAPACITY:512}
    session-queue-capacity: ${COLLA_REALTIME_SESSION_QUEUE_CAPACITY:64}
    recent-signal-capacity: ${COLLA_REALTIME_RECENT_SIGNAL_CAPACITY:10000}
    legacy-frames-enabled: ${COLLA_REALTIME_LEGACY_FRAMES_ENABLED:true}
    legacy-knowledge-inbound-policy: ${COLLA_REALTIME_LEGACY_KNOWLEDGE_INBOUND_POLICY:reject}
    shutdown-timeout: ${COLLA_REALTIME_SHUTDOWN_TIMEOUT:5s}
  events:
    worker:
      enabled: ${COLLA_EVENT_WORKER_ENABLED:false}
      poll-interval: ${COLLA_EVENT_WORKER_POLL_INTERVAL:1s}
      recovery-interval: ${COLLA_EVENT_WORKER_RECOVERY_INTERVAL:15s}
      heartbeat-interval: ${COLLA_EVENT_WORKER_HEARTBEAT_INTERVAL:10s}
      shutdown-grace: ${COLLA_EVENT_WORKER_SHUTDOWN_GRACE:25s}
      claim-batch: ${COLLA_EVENT_WORKER_CLAIM_BATCH:20}
      concurrency: ${COLLA_EVENT_WORKER_CONCURRENCY:4}
      queue-capacity: ${COLLA_EVENT_WORKER_QUEUE_CAPACITY:16}
      connection-budget: ${COLLA_EVENT_WORKER_CONNECTION_BUDGET:6}
      expected-instances: ${COLLA_EVENT_WORKER_EXPECTED_INSTANCES:2}
      postgresql-connection-budget: ${COLLA_POSTGRESQL_CONNECTION_BUDGET:100}
    delivery:
      lease-duration: ${COLLA_EVENT_LEASE_DURATION:30s}
      max-execution-duration: ${COLLA_EVENT_MAX_EXECUTION_DURATION:10m}
      base-retry-delay: ${COLLA_EVENT_BASE_RETRY_DELAY:2s}
      max-retry-delay: ${COLLA_EVENT_MAX_RETRY_DELAY:5m}
      max-attempts: ${COLLA_EVENT_MAX_ATTEMPTS:8}
      max-claim-batch: ${COLLA_EVENT_MAX_CLAIM_BATCH:100}
      max-maintenance-batch: ${COLLA_EVENT_MAX_MAINTENANCE_BATCH:100}
  storage:
    endpoint: http://localhost:9000
    access-key: colla_minio
    secret-key: colla_minio_password
    bucket: colla-files
    max-upload-size-bytes: 104857600
  security:
    access-secret: change-me-access
    refresh-secret: change-me-refresh
    access-token-ttl-minutes: 30
    refresh-token-ttl-days: 30
  web-security:
    cors-allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
  password-policy:
    min-length: 8
    require-letter: true
    require-digit: true
  init-admin:
    username: admin
    password: admin123456
    display-name: Administrator
    email: admin@colla.local
  project-space-experience:
    enabled: ${COLLA_PROJECT_SPACE_EXPERIENCE_ENABLED:true}
    kill-switch: ${COLLA_PROJECT_SPACE_EXPERIENCE_KILL_SWITCH:false}
    policy-version: ${COLLA_PROJECT_SPACE_EXPERIENCE_POLICY_VERSION:s21-m7-v1}
    rollout-basis-points: ${COLLA_PROJECT_SPACE_EXPERIENCE_ROLLOUT_BASIS_POINTS:10000}
    evaluation-salt: ${COLLA_PROJECT_SPACE_EXPERIENCE_EVALUATION_SALT:s21-m7-local-rollout}
    cache-max-age-seconds: ${COLLA_PROJECT_SPACE_EXPERIENCE_CACHE_MAX_AGE_SECONDS:30}
    telemetry:
      enabled: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_ENABLED:true}
      sample-basis-points: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_SAMPLE_BASIS_POINTS:10000}
      max-batch-size: ${COLLA_PROJECT_SPACE_EXPERIENCE_TELEMETRY_MAX_BATCH_SIZE:20}
  knowledge-collaboration:
    public-url: ${COLLA_COLLABORATION_PUBLIC_URL:ws://localhost:1234}
    internal-url: ${COLLA_COLLABORATION_INTERNAL_URL:http://localhost:1234}
    internal-secret: ${COLLA_COLLABORATION_INTERNAL_SECRET:colla-local-collaboration-secret}
    ticket-ttl: 5m
    max-update-bytes: 1048576
    retained-updates: 100
    expired-ticket-retention: 1h
