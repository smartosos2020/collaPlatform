# Archived Windows PowerShell Operations

Archived on 2026-07-18. Active operations live in `tools/workbench/src/operations` and are exposed through root `pnpm ops:*` commands. These files are read-only historical evidence.
